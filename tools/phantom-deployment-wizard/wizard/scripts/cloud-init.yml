#cloud-config
package_update: true
package_upgrade: true

packages:
  - curl

runcmd:
  - curl -sSL https://install.phantom.tc | bash 2>&1 | tee /var/log/phantom-install.log
  - echo "Phantom-WG installation completed at $(date)" >> /var/log/phantom-install.log