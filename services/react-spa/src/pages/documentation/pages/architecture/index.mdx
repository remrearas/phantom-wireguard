import Mermaid from '@shared/components/visualization/Mermaid';
import { InlineNotification } from '@carbon/react';

# Sistem Mimarisi

***Phantom-WG***, WireGuard® tabanlı bir VPN yönetim platformudur. Tüm bileşenler Docker container yapısı içerisinde çalışır ve asıl işi yapan ***Phantom Daemon*** Unix Domain Socket (UDS) üzerinden haberleşir — daemon hiçbir TCP portunu doğrudan internete açmaz.
Üretim topolojisindeki yapı üzerinde daemon tarafına sorgu proxy katmanı ile iletilmektedir. Asıl işi yapan parça ***Phantom Daemon***, senaryolarınızda farklı biçimlerde de kullanılabilir, UDS üzerinden nasıl haberleşeceğini bildiğiniz yapıyı konfigürasyonunuza göre esnetebilir, değiştirebilirsiniz.

---

## Üretim Topolojisi

<Mermaid chart={`flowchart TD
  B["Tarayıcı"] -->|"HTTPS :443"| N
  subgraph Docker["Container Architecture"]
    N["nginx\n(TLS)"]
    N -->|"/auth/*"| A["auth-service\n(JWT)"]
    N -->|"/*"| S["SPA\n(React)"]
    A -->|"/api/* (Proxy) · UDS"| D["Phantom Daemon"]
  end
  subgraph WG["WireGuard :51820/udp"]
    C1["İstemci 1"]
    C2["İstemci 2"]
    CN["İstemci N"]
  end
  WG -->|"UDP :51820"| D
`} />

İki bağımsız giriş noktası:

<Mermaid chart={`flowchart LR
  A[":443 HTTPS"] --> N["nginx"] --> AS["auth-service"] --> D["Phantom Daemon"]
  B[":51820 UDP"] --> D
`} />

---

## Bridge Mimarisi

Daemon, sistem düzeyindeki işlemleri native bridge'ler aracılığıyla gerçekleştirir. Python katmanı iş mantığını yönetir, bridge'ler ise kernel ile doğrudan iletişim kurar.

<Mermaid chart={`flowchart TD
  D["Phantom Daemon\n(Python)"]
  D -->|"FFI"| WGB["wireguard-go-bridge\n(Go)"]
  D -->|"FFI"| FWB["firewall-bridge\n(Rust)"]
  WGB -->|"tun.CreateTUN"| TUN["/dev/net/tun\nWireGuard arayüzleri"]
  FWB -->|"nftables API"| NFT["nftables\nKernel kuralları"]
`} />

| Bridge                  | Dil  | Sorumluluk                                                               |
|-------------------------|------|--------------------------------------------------------------------------|
| **wireguard-go-bridge** | Go   | WireGuard userspace, TUN arayüz oluşturma, peer yönetimi, UAPI protokolü |
| **firewall-bridge**     | Rust | nftables kural grupları, routing policy, atomik kural uygulaması         |

---

## Docker Compose Yapısı

```yaml
# phantom-daemon  ·  Production Compose
# Topology:
#   Internet → :443 (nginx, TLS) → auth-service:8443 → daemon (UDS)
#   Internet → :51820/udp (WireGuard) → daemon

services:
  daemon:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: phantom-daemon
    restart: unless-stopped
    volumes:
      - phantom-sock:/var/run/phantom
      - ./container-data/db:/var/lib/phantom/db
      - ./container-data/state/db:/var/lib/phantom/state/db
    secrets:
      - wg_private_key
      - wg_public_key
    environment:
      - PHANTOM_DB_DIR=/var/lib/phantom/db          # client/peer database
      - PHANTOM_STATE_DIR=/var/lib/phantom/state/db  # runtime state database
      - WIREGUARD_LISTEN_PORT=51820     # UDP listen port
      - WIREGUARD_MTU=1420              # interface MTU
      - WIREGUARD_KEEPALIVE=25          # persistent keepalive (seconds)
      - WIREGUARD_ENDPOINT_V4=          # public IPv4 (required for clients)
      - WIREGUARD_ENDPOINT_V6=          # public IPv6 (optional)
    cap_add:
      - NET_ADMIN
      - NET_RAW
    sysctls:
      - net.ipv4.ip_forward=1
    devices:
      - /dev/net/tun:/dev/net/tun
    ports:
      - "51820:51820/udp"

  auth-service:
    build:
      context: ./services/auth-service
      dockerfile: Dockerfile
    container_name: phantom-auth
    restart: unless-stopped
    depends_on:
      - daemon
    volumes:
      - phantom-sock:/var/run/phantom
      - ./container-data/auth-db:/var/lib/phantom/auth
    secrets:
      - auth_signing_key
      - auth_verify_key
    environment:
      - PHANTOM_DAEMON_SOCKET=/var/run/phantom/daemon.sock
      - AUTH_DB_DIR=/var/lib/phantom/auth      # SQLite auth.db location
      - AUTH_SECRETS_DIR=/run/secrets          # signing/verify key directory
      - AUTH_TOKEN_LIFETIME=86400              # JWT expiry (seconds, default: 24h)
      - AUTH_MFA_TOKEN_LIFETIME=300            # MFA pending token (seconds, default: 5m)
      - AUTH_INACTIVITY_TIMEOUT=1800           # session timeout (seconds, default: 30m)
      - AUTH_RATE_LIMIT_WINDOW=60              # sliding window (seconds)
      - AUTH_RATE_LIMIT_MAX=5                  # max attempts per window

  nginx:
    image: nginx:1-alpine
    container_name: phantom-nginx
    restart: unless-stopped
    depends_on:
      - auth-service
    volumes:
      - ./services/nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    secrets:
      - tls_cert
      - tls_key
    ports:
      - "443:443"

secrets:
  wg_private_key:
    file: ./container-data/secrets/production/wg_private_key
  wg_public_key:
    file: ./container-data/secrets/production/wg_public_key
  auth_signing_key:
    file: ./container-data/secrets/production/auth_signing_key
  auth_verify_key:
    file: ./container-data/secrets/production/auth_verify_key
  tls_cert:
    file: ./container-data/secrets/production/tls_cert
  tls_key:
    file: ./container-data/secrets/production/tls_key

volumes:
  phantom-sock:
```

---

## İstek Yaşam Döngüsü

Bir API isteğinin sistemden geçiş süreci:

<Mermaid chart={`sequenceDiagram
  participant B as Tarayıcı (SPA)
  participant N as nginx (:443)
  participant A as auth-service
  participant D as daemon (UDS)

  B->>N: HTTPS GET /api/core/wireguard
  N->>A: Proxy /api/*
  A->>A: JWT doğrula (Bearer token)
  A->>A: Denetim kaydı (proxy_request)
  A->>D: UDS iletim (daemon.sock)
  D->>D: İşlem (WireGuard IPC / nftables API)
  D-->>A: JSON yanıt
  A-->>N: JSON yanıt
  N-->>B: HTTPS yanıt
`} />

<InlineNotification
  kind="info"
  title="Güvenlik katmanları."
  subtitle="Her istek 3 katmandan geçer: Nginx (TLS) → Auth Service (JWT) → Phantom Daemon (Unix Socket)"
  hideCloseButton
  lowContrast
/>

---

## Veritabanları

Tüm veritabanları SQLite formatındadır. İki kategoriye ayrılır:

### Kalıcı Veriler

Kullanıcı verileri ve yapılandırma. Yedekleme ve taşıma bu veritabanları üzerinden yapılır.

| Veritabanı    | Sahip        | Konteyner Yolu        |
|---------------|--------------|-----------------------|
| **wallet.db** | daemon       | /var/lib/phantom/db   |
| **exit.db**   | daemon       | /var/lib/phantom/db   |
| **auth.db**   | auth-service | /var/lib/phantom/auth |

### Çalışma Zamanı (Runtime) Durumu

Bridge'ler tarafından yönetilen çalışma zamanı verileri. Daemon başlatıldığında otomatik oluşturulur, müdahale gerektirmez.

| Veritabanı      | Sahip               | Konteyner Yolu                                  |
|-----------------|---------------------|-------------------------------------------------|
| **device.db**   | wireguard-go-bridge | /var/lib/phantom/state/db/wireguard/\{ifname\}/ |
| **firewall.db** | firewall-bridge     | /var/lib/phantom/state/db                       |


