import GhostMode from '@pages/vpn/ghost/shared/components/GhostMode';

# Ghost Mode

Ghost Mode tunnels WireGuard traffic over a WebSocket/TLS connection, making it appear as standard HTTPS traffic.

---

<GhostMode />
